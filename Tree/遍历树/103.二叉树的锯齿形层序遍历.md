# 103.二叉树的锯齿形层序遍历
```js
var zigzagLevelOrder = function(root) {
  const arr = []
  const func = (node, level) => {
    if (!node) return 

    if (!arr[level]) arr[level] = []
    arr[level].push(node.val)

    
    func(x & 1 === 1 ? node.right : node.left, level + 1)
    func(x & 1 === 1 ? node.left : node.right, level + 1)
  }
  func(root, 0)
  return arr
};
```